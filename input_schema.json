{
  "title": "Email Scraper - Homepage Only",
  "type": "object",
  "schemaVersion": 1,
  "properties": {
    "urls": {
      "title": "URLs to Scrape",
      "type": "array",
      "description": "List of homepage URLs to scrape for emails. Only the exact URLs provided will be scraped.",
      "editor": "json"
    },
    "url": {
      "title": "Single URL",
      "type": "string",
      "description": "Single homepage URL to scrape for emails.",
      "editor": "textfield"
    },
    "waitForContent": {
      "title": "Wait for Dynamic Content (seconds)",
      "type": "integer",
      "description": "How long to wait for dynamic content to load on each page",
      "default": 3,
      "minimum": 1,
      "maximum": 10,
      "editor": "number"
    }
  },
  "required": []
}
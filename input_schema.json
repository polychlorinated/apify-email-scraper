{
  "title": "Email Scraper",
  "type": "object",
  "schemaVersion": 1,
  "properties": {
    "urls": {
      "title": "URLs to Scrape",
      "type": "array",
      "description": "List of URLs to scrape for emails. You can provide either this field OR the single URL field below, but not both.",
      "editor": "json"
    },
    "url": {
      "title": "Single URL",
      "type": "string",
      "description": "Single URL to scrape for emails. Use this if you only have one URL, otherwise use the URLs array above.",
      "editor": "textfield",
      "pattern": "^https?://"
    }
  },
  "required": []
}